# GitHub Task Unisuam

Repositório task,

# Acessar o container

cd docker
docker-compose exec -it php8 bash

# Instalar as dependencias

cd html
composer install && npm install

# Migrations

php artisan migrate
